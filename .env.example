# apps/api
DATABASE_URL=postgresql://rentledger:rentledger@localhost:5435/rentledger?schema=public
PORT=4000
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=change-me-to-a-long-random-string
# Set COOKIE_SECURE=false when serving production over plain http (e.g. LAN)
#COOKIE_SECURE=false
# Where entry receipt photos are stored (defaults to ./uploads in dev)
#RECEIPTS_DIR=/data/receipts

# apps/web
NEXT_PUBLIC_API_URL=http://localhost:4000/api
